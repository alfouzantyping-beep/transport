import { prisma } from "@/lib/db";
import { saveCompany } from "@/lib/master-data";

export default async function CompanySettingsPage() {
  const company = await prisma.company.findFirst();
  const input = "rounded-lg border border-slate-200 px-3 py-2 text-sm outline-none focus:border-emerald-500";
  return <div className="space-y-5"><div><h1 className="text-2xl font-black text-slate-950">Company Settings</h1><p className="text-sm text-slate-500">Used later for invoices and reports.</p></div><form action={saveCompany} className="grid gap-4 rounded-lg border border-slate-200 bg-white p-5 shadow-sm md:grid-cols-2"><label className="grid gap-1 text-sm font-bold text-slate-700">Company Name<input className={input} name="companyName" defaultValue={company?.companyName || ""} required /></label><label className="grid gap-1 text-sm font-bold text-slate-700">Trade License No<input className={input} name="tradeLicenseNo" defaultValue={company?.tradeLicenseNo || ""} required /></label><label className="grid gap-1 text-sm font-bold text-slate-700">TRN<input className={input} name="trn" defaultValue={company?.trn || ""} required /></label><label className="grid gap-1 text-sm font-bold text-slate-700">Phone<input className={input} name="phone" defaultValue={company?.phone || ""} required /></label><label className="grid gap-1 text-sm font-bold text-slate-700">Email<input className={input} type="email" name="email" defaultValue={company?.email || ""} required /></label><label className="grid gap-1 text-sm font-bold text-slate-700">Website<input className={input} name="website" defaultValue={company?.website || ""} /></label><label className="grid gap-1 text-sm font-bold text-slate-700 md:col-span-2">Address<textarea className={input} name="address" defaultValue={company?.address || ""} required /></label><label className="grid gap-1 text-sm font-bold text-slate-700">Logo URL<input className={input} name="logoUrl" defaultValue={company?.logoUrl || ""} /></label><label className="grid gap-1 text-sm font-bold text-slate-700">Bank Name<input className={input} name="bankName" defaultValue={company?.bankName || ""} /></label><label className="grid gap-1 text-sm font-bold text-slate-700">IBAN<input className={input} name="iban" defaultValue={company?.iban || ""} /></label><label className="grid gap-1 text-sm font-bold text-slate-700">Account No<input className={input} name="accountNo" defaultValue={company?.accountNo || ""} /></label><div className="md:col-span-2"><button className="rounded-lg bg-emerald-600 px-5 py-2 text-sm font-bold text-white hover:bg-emerald-700">Save Company Settings</button></div></form></div>;
}
