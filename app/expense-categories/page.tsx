import { prisma } from "@/lib/db";
import { saveExpenseCategory } from "@/lib/master-data";

export default async function ExpenseCategoriesPage() {
  const categories = await prisma.expenseCategory.findMany({ orderBy: { name: "asc" } });
  const input = "rounded-lg border border-slate-200 px-3 py-2 text-sm outline-none focus:border-emerald-500";
  return <div className="grid gap-5 xl:grid-cols-[360px_1fr]"><div className="space-y-3"><h1 className="text-2xl font-black text-slate-950">Expense Categories</h1><form action={saveExpenseCategory} className="grid gap-3 rounded-lg border border-slate-200 bg-white p-5 shadow-sm"><label className="grid gap-1 text-sm font-bold text-slate-700">Name<input className={input} name="name" required /></label><label className="grid gap-1 text-sm font-bold text-slate-700">Type<select className={input} name="type" defaultValue="TRIP"><option value="TRIP">Trip</option><option value="VEHICLE">Vehicle</option><option value="DRIVER">Driver</option><option value="GENERAL">General</option></select></label><label className="grid gap-1 text-sm font-bold text-slate-700">Status<select className={input} name="status" defaultValue="ACTIVE"><option value="ACTIVE">Active</option><option value="INACTIVE">Inactive</option></select></label><label className="grid gap-1 text-sm font-bold text-slate-700">Description<textarea className={input} name="description" /></label><button className="rounded-lg bg-emerald-600 px-4 py-2 text-sm font-bold text-white">Add Category</button></form></div><div className="overflow-hidden rounded-lg border border-slate-200 bg-white shadow-sm"><table className="w-full text-left text-sm"><thead className="bg-slate-50 text-xs font-bold uppercase tracking-wide text-slate-500"><tr><th className="p-3">Name</th><th className="p-3">Type</th><th className="p-3">Status</th></tr></thead><tbody className="divide-y divide-slate-100">{categories.map((category) => <tr key={category.id}><td className="p-3 font-bold text-slate-900">{category.name}<div className="text-xs font-medium text-slate-500">{category.description}</div></td><td className="p-3">{category.type}</td><td className="p-3">{category.status}</td></tr>)}</tbody></table></div></div>;
}
